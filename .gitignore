# Dependências instaladas por npx npm install - nunca versionar
node_modules/

# Ambiente Node local (criado por nodeenv) - cada um gera o seu.
# Contém o binário do Node inteiro (mais de 100 MB): nunca versionar.
.nodeenv/

# Saídas de build
dist/
dist-electron/
release/

# Credenciais (Módulo 6 em diante: string de conexão do PostgreSQL)
.env

# Ruído do sistema e logs
*.log
.DS_Store